apiVersion: v1
kind: Service
metadata:
  name: ehes-dashboard-service
  namespace: ehes-app
  labels:
    app: ehes-dashboard
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: ehes-dashboard
